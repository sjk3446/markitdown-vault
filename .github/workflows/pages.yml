name: Deploy public interface

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Prepare static site
        run: |
          mkdir -p _site/static
          cp web/index.html _site/index.html
          cp web/styles.css _site/static/styles.css
          cp web/app.js _site/static/app.js
          cp web/browser-vault.js _site/static/browser-vault.js
          cp web/microsoft-logo.png _site/static/microsoft-logo.png
          cp -R web/vendor _site/static/vendor
          cp web/manifest.webmanifest _site/manifest.webmanifest
          cp web/icon.svg _site/icon.svg
          cp web/sw.js _site/sw.js
          cp THIRD_PARTY_NOTICES.md _site/THIRD_PARTY_NOTICES.md
          touch _site/.nojekyll
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
