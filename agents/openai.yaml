interface:
  display_name: "MarkItDown Vault"
  short_description: "Convert and archive files as searchable Markdown"
  default_prompt: "Use $markitdown-vault to convert my files into a categorized local Markdown vault."
policy:
  allow_implicit_invocation: true
